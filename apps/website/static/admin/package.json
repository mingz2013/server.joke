{
  "name": "admin",
  "version": "1.0.0",
  "description": "admin",
  "main": "webpack.config.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm i && webpack --progress --profile --colors --display-error-details --display-cached",
    "publish": "npm i && webpack -p --progress --profile --colors --display-error-details --display-cached",
    "start": "npm i && webpack && node ./node_modules/http-server/bin/http-server . -p 8080 -o",
    "watch": "npm i && webpack -d --watch"
  },
  "author": "mingz",
  "license": "ISC",
  "devDependencies": {
    "babel-core": "^6.18.2",
    "babel-loader": "^6.2.7",
    "babel-preset-es2015": "^6.18.0",
    "css-loader": "^0.25.0",
    "file-loader": "^0.9.0",
    "http-server": "^0.9.0",
    "style-loader": "^0.13.1",
    "url-loader": "^0.5.7",
    "webpack": "^1.13.3"
  },
  "dependencies": {
    "jquery": "^3.1.1"
  }
}
